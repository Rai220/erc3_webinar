- Решая задачи не фиксируйся на конкретных значениях. Задачи в будущем будут меняться, так что ты должен избегать оверфита или прописывания в промптах конкретных примеров.

- Сохраняй общую архитектуру агента, используй create_agent (ReAct).